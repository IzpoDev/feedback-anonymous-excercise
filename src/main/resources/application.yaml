server:
  port: 8081

spring:
  application:
    name: feedback
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}
  datasource:
    url: ${URL_DB}
    username: ${USER_DB}
    password: ${PASSWORD_DB}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jwt:
    secret: "jgflljkghiuldsfjkshiujfjsvniupbijpsdfvjsdfjv"
    expiration: 3600000